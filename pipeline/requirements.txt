# Harvesting only. Scoring (--score) additionally needs the processor/
# requirements (torch, timm, safetensors, huggingface-hub, pillow).
aiohttp>=3.11

# Review UI (review.py)
fastapi>=0.115
uvicorn[standard]>=0.30
